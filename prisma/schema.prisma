generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model AdminUser {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String
  password  String
  role      String   @default("admin")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("avora_admin_users")
}

model PageContent {
  id          String   @id @default(uuid())
  slug        String   @unique
  title       String
  description String?  @db.Text
  sections    String   @db.LongText
  metaTitle   String?
  metaDesc    String?  @db.Text
  isPublished Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("avora_pages")
}

model Blog {
  id          String   @id @default(uuid())
  slug        String   @unique
  title       String
  excerpt     String   @db.Text
  content     String   @db.LongText
  category    String   @default("Technology")
  tags        String   @default("[]")
  coverImage  String?
  authorName  String   @default("Avora Engineering")
  authorRole  String   @default("Tech Lead")
  readTime    String   @default("5 min read")
  isFeatured  Boolean  @default(false)
  isPublished Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("avora_blogs")
}

model Inquiry {
  id        String   @id @default(uuid())
  name      String
  email     String
  phone     String?
  company   String?
  service   String?
  budget    String?
  timeline  String?
  message   String   @db.Text
  status    String   @default("new")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("avora_inquiries")
}

model ContactSubmission {
  id        String   @id @default(uuid())
  name      String
  email     String
  subject   String?
  message   String   @db.Text
  phone     String?
  createdAt DateTime @default(now())

  @@map("avora_contact_submissions")
}

model Subscriber {
  id        String   @id @default(uuid())
  email     String   @unique
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  @@map("avora_subscribers")
}

model Setting {
  id        String   @id @default(uuid())
  key       String   @unique
  value     String   @db.LongText
  updatedAt DateTime @updatedAt

  @@map("avora_settings")
}

